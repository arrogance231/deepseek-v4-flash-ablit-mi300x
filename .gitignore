# Local runtime state (bind mounts referenced by compose.yaml)
aiter-cache/
crash-dumps/
profiles-current/

# Generated Phase-3 corpora, probe JSON, and calibrated sidecar weights.
# These are reproducible local artifacts and must not enter the public repo.
results/raw/

# Expanded by prepare-artifacts.sh from artifacts/ (SHA-256 verified)
/patches/_C_stable_libtorch*.so

# Your real Caddyfile contains a hostname, email, and source CIDR.
# Copy Caddyfile.example to Caddyfile and edit it.
Caddyfile
.env

# Editor/OS noise
.DS_Store
*.swp
*.swo
__pycache__/
kernel-dev/hip-a8w4/*_hip.hip
