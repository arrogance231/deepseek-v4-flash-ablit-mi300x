--- a/vllm/v1/worker/gpu/spec_decode/utils.py
+++ b/spec-decode-utils.independent-draft-gumbel.py
@@ -7,6 +7,13 @@
 from vllm.v1.worker.gpu.async_utils import async_copy_to_np
 from vllm.v1.worker.gpu.input_batch import InputBatch
 
+# Keep draft proposal noise independent from rejection acceptance/recovery noise.
+DRAFT_GUMBEL_POS_OFFSET = 1 << 30
+
+
+def draft_gumbel_pos(positions: torch.Tensor) -> torch.Tensor:
+    return positions + (1 + DRAFT_GUMBEL_POS_OFFSET)
+
 
 class DraftTokensHandler:
     def __init__(self, device: torch.device | None = None):
