--- a/vllm/model_executor/layers/activation.py
+++ b/patches/activation.rocm-exact-swiglu.py
@@ -3,19 +3,43 @@
 """Custom activation functions."""
 
 import math
+import os
 
 import torch
 import torch.nn as nn
 import torch.nn.functional as F
 
+from vllm.distributed import (
+    divide,
+    get_tensor_model_parallel_rank,
+    get_tensor_model_parallel_world_size,
+)
 from vllm.logger import init_logger
 from vllm.model_executor.custom_op import CustomOp
+from vllm.model_executor.utils import set_weight_attrs
 from vllm.platforms import CpuArchEnum, current_platform
 from vllm.triton_utils import tl, triton
 from vllm.utils.collection_utils import LazyDict
 
 logger = init_logger(__name__)
 
+_rocm_swiglu_clamp = None
+
+
+def _load_rocm_swiglu_clamp():
+    global _rocm_swiglu_clamp
+    if _rocm_swiglu_clamp is None:
+        os.environ["PYTORCH_ROCM_ARCH"] = "gfx942"
+        from torch.utils.cpp_extension import load
+
+        root = "/opt/cj-moe"
+        _rocm_swiglu_clamp = load(
+            "cj_swiglu_clamp",
+            [f"{root}/swiglu_clamp.cpp", f"{root}/swiglu_clamp.hip"],
+            extra_cuda_cflags=["-O3", "--offload-arch=gfx942"],
+        )
+    return _rocm_swiglu_clamp
+
 
 @triton.jit
 def _swiglustep_and_mul_kernel(
@@ -226,7 +250,9 @@
         self.swiglu_limit = float(swiglu_limit)
         self.alpha = float(alpha)
         self.beta = float(beta)
-        if current_platform.is_rocm() or current_platform.is_xpu():
+        if current_platform.is_rocm():
+            self._forward_method = self.forward_rocm
+        elif current_platform.is_xpu():
             self._forward_method = self.forward_native
         elif current_platform.is_cuda_alike():
             self.op = torch.ops._C.silu_and_mul_with_clamp
@@ -246,6 +272,14 @@
         self.op(out, x, self.swiglu_limit, self.alpha, self.beta)
         return out
 
+    def forward_rocm(self, x: torch.Tensor) -> torch.Tensor:
+        d = x.shape[-1] // 2
+        out = torch.empty(x.shape[:-1] + (d,), dtype=x.dtype, device=x.device)
+        _load_rocm_swiglu_clamp().run(
+            out, x, self.swiglu_limit, self.alpha, self.beta
+        )
+        return out
+
     def forward_xpu(self, x: torch.Tensor) -> torch.Tensor:
         return self.forward_native(x)
 
@@ -782,6 +816,48 @@
         return self.forward_native(input)
 
 
+class ScaledActivation(nn.Module):
+    """An activation function with post-scale parameters.
+
+    This is used for some quantization methods like AWQ.
+    """
+
+    def __init__(
+        self,
+        act_module: nn.Module,
+        intermediate_size: int,
+        input_is_parallel: bool = True,
+        params_dtype: torch.dtype | None = None,
+    ):
+        super().__init__()
+        self.act = act_module
+        self.input_is_parallel = input_is_parallel
+        if input_is_parallel:
+            tp_size = get_tensor_model_parallel_world_size()
+            intermediate_size_per_partition = divide(intermediate_size, tp_size)
+        else:
+            intermediate_size_per_partition = intermediate_size
+        if params_dtype is None:
+            params_dtype = torch.get_default_dtype()
+        self.scales = nn.Parameter(
+            torch.empty(intermediate_size_per_partition, dtype=params_dtype)
+        )
+        set_weight_attrs(self.scales, {"weight_loader": self.weight_loader})
+
+    def forward(self, x: torch.Tensor) -> torch.Tensor:
+        return self.act(x) / self.scales
+
+    def weight_loader(self, param: nn.Parameter, loaded_weight: torch.Tensor):
+        param_data = param.data
+        if self.input_is_parallel:
+            tp_rank = get_tensor_model_parallel_rank()
+            shard_size = param_data.shape[0]
+            start_idx = tp_rank * shard_size
+            loaded_weight = loaded_weight.narrow(0, start_idx, shard_size)
+        assert param_data.shape == loaded_weight.shape
+        param_data.copy_(loaded_weight)
+
+
 _ACTIVATION_REGISTRY = LazyDict(
     {
         "gelu": lambda: GELU(),
